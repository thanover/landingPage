<!DOCTYPE html>
<html lang="en">
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1"/>
    <link rel="preconnect" href="https://fonts.googleapis.com"/>
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>
    <link
      href="https://fonts.googleapis.com/css2?family=Source+Code+Pro:ital,wght@0,400;0,500;0,600;0,800;1,400;1,500;1,700;1,800&display=swap"
      rel="stylesheet"/>
    <link rel="stylesheet" href="/css/tailwind.css"/>
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest">
    <link rel="mask-icon" href="/safari-pinned-tab.svg" color="#5bbad5">
    <meta name="msapplication-TileColor" content="#da532c">
    <meta name="theme-color" content="#ffffff">
    <title>{{ global.name }}</title>
  </head>
  {# <body class="h-screen w-screen bg-gradient-to-r from-{{ global.style.gradientLeft }} to-{{ global.style.gradientRight }}
          text-slate-200 font-SourceCodePro"> #}
  <body class="h-screen w-screen bg-gradient-to-r from-cyan-700 to-sky-900
                  text-slate-200 font-SourceCodePro">
    <div class="flex justify-center p-6">
      {% image "rounded-full flex w-56 content-center justify-center",
    "src/images/profile_image3.jpeg", "profile picture" %}
    </div>
    <div class="flex justify-center">
      <p class="font-bold text-4xl sm:text-3xl">
        {{ global.name }}
      </p>
    </div>
    <div class="flex justify-center">
      <p class="text-base sm:text-sm pb-10">
        {{ global.tags }}
      </p>
    </div>
    {% for link in global.links %}
      <div class="flex justify-center">
        <a class="w-1/2 sm:w-64 flex justify-center items-center m-3 p-3 sm:p-2 sm:m-2 bg-cyan-700 shadow-lg rounded-lg hover:bg-cyan-600" href="{{ link.link }}">
          <img class="fill-black stroke-black size-8 sm:size-6" src="{{ link.iconPath }}"/>
          <p class="pl-4 text-lg sm:text-base">
            {{ link.text }}
          </p>
        </a>
      </div>
    {% endfor %}
  </body>
</html>